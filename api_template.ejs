<% _.forEach(apis, function(api){ %>
## <%= api.returnType %> <%= api.name %>

include: <%= api.belong %>

desc: <%= i18n(api.description) %>

**params:**
<% if(api.fields && api.fields.length > 0){ %>
<% _.forEach(api.fields, function(field){ %>
\<<%= field.type %>\><%= field.name %>: <%= i18n(field.description) %>

<% }); %>
<% } %>
<% _.forEach(api.examples, function(example, index){ %>
### example<%= index %>
<%= example.example %>

<%= i18n(example.description) %>

<% }); %>

<% }); %>